This directory contains a prototype sidebar that did not end up being implemented due to time constraints. To test it, simply run site.php.

IMPORTANT: This prototype is NOT compatible with our current implementation, so if we want to go in this direction, the sidebar would have to be implemented from scratch (this time using bootstrap to ensure compatibility with the rest of the system), and this prototype would be used ONLY as a reference.
